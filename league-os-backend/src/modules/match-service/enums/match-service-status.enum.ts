export enum MatchServiceStatus {
  NOT_STARTED = 'not_started',
  FIRST_HALF = 'first_half',
  HALF_TIME = 'half_time',
  SECOND_HALF = 'second_half',
  PAUSED = 'paused',
  EVENT_RECORDING = 'event_recording',
  FINISHED = 'finished',
  PROTOCOL_SIGNED = 'protocol_signed',
}